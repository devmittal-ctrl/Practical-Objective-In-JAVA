// both leetcode problems are solved here
    static int hammingWeightShift(int n) {
        int count = 0;

        for (int i = 0; i < 32; i++) {
            count += n & 1;
            n >>>= 1;
        }

        return count;
    }

    static int hammingWeightFast(int n) {
        int count = 0;

        while (n != 0) {
            n &= n - 1;
            count++;
        }

        return count;
    }

    

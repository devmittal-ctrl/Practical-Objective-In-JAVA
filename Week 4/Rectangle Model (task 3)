public class zolo_lab{
    public void use(){
        // task 3 : rectangle model
        System.out.println("pls enter the positive dimensions");
        Rectangle r1=new Rectangle(4);
        r1.getArea();
        r1.getPreimeter();
        r1.isSquare(r1);
        String h=r1.toString();
        System.out.println(h);
    } 
}
class Rectangle{
    private double width;
    private double height;
    Rectangle(){
        this(1,1);
        System.out.println("no argument constructor pased");
        System.out.println("height of rectangle : "+height+"width of rectangle : "+width);
    }
    Rectangle(double dime){
        height=dime;
        width=dime;
        if(dime>=0){
        System.out.println("one argument constructor is passed ");
        System.out.println("height of rectangle : "+height+" width of rectangle : "+width);
        }
        else System.err.println("pls enter a valid dimension");
    }
    Rectangle(double t,double r){
        System.out.println("two argument constructor is passed");
        if(t>=0&&r>=0){
        height=t;
        width=r;
        System.out.println("heigth of the rectangle is : "+height+" width of rectangle is : "+width);
        }
        else System.err.println("pls enter a valid dimension");

    }
    double getArea(){
        return width*height;
    }
    double getPreimeter(){
        return 2*(height+width);
    }
    boolean isSquare(Rectangle r){
        if(width==height){
            System.out.println("yes its a square");
            return true;
        }
        else {
        System.out.println("no its not a square");
        return false;
            }
    }
    String toString(Rectangle r){
        return "Rectangle{width= "+width+", heigth= "+height+"}";
    }
}
